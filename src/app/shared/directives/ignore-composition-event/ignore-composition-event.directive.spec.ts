import { IgnoreCompositionEventDirective } from './ignore-composition-event.directive';

describe('IgnoreCompositionEventDirective', () => {
  it('should create an instance', () => {
    // const directive = new IgnoreCompositionEventDirective();
    // expect(directive).toBeTruthy();
  });
});
