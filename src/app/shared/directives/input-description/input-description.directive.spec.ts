import { InputDescriptionDirective } from './input-description.directive';

describe('InputDescriptionDirective', () => {
  it('should create an instance', () => {
    // const directive = new InputDescriptionDirective();
    // expect(directive).toBeTruthy();
  });
});
